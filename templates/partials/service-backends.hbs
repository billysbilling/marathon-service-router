# Backends for all services

{{#each services}}
backend {{serviceId}}
  mode http
  balance roundrobin{{#each tasks}}
  server {{taskId}} {{host}}:{{port}}{{/each}}

backend {{serviceId}}-tcp
  mode tcp
  balance roundrobin{{#each tasks}}
  server {{taskId}} {{host}}:{{port}}{{/each}}

{{/each}}
