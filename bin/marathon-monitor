#!/usr/bin/env node

require('babel-polyfill')

var main = require('../lib/main').default

console.log('Marathon monitor started')

main()

process.on('unhandledRejection', function(e) {
    console.error('Unhandled rejection:')
    console.error(e.stack || e.message)
    process.exit(1)
})
